from setuptools import setup, find_packages

setup(name='Wykan',
      version='1.0',
      description='Python wrapping for wekan rest API.',
      packages=find_packages(),
      python_requires=">=3"
      )
